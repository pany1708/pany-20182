spring:
  application:
    name: ${pre.spring.application.name:aggregator-dubbo}
server:
  port: ${pre.server.port:9506}
security:
  basic:
    enabled: ${pre.security.basic.enabled:false}
eureka:
  client:
    serviceUrl:
      defaultZone: ${pre.eureka.client.serviceUrl.defaultZone:http://slave1:10000/eureka/,http://slave2:10000/eureka/,http://slave3:10000/eureka/}
  instance:
    prefer-ip-address: true

turbine:
  appConfig:
    provider-user-dubbo,
    provider-member-regist-dubbo,
    provider-cart-dubbo,
    provider-order-dubbo,
    provider-goods-dubbo,
    provider-payment-dubbo,
    provider-income-dubbo

 #aggregator:
     #clusterConfig: provider-user-dubbo,provider-cart-dubbo
  clusterNameExpression: ${pre.turbine.clusterNameExpression:new String("default")}
  combine-host-port: ${pre.turbine.combine-host-port:true}




    
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kingthy.mapper.SourceCategoryMapper" >
  <resultMap id="BaseResultMap" type="com.kingthy.entity.SourceCategory" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="create_date" property="createDate" jdbcType="TIMESTAMP" />
    <result column="modify_date" property="modifyDate" jdbcType="TIMESTAMP" />
    <result column="version" property="version" jdbcType="INTEGER" />
    <result column="creator" property="creator" jdbcType="VARCHAR" />
    <result column="modifier" property="modifier" jdbcType="VARCHAR" />
    <result column="parent_id" property="parentId" jdbcType="CHAR" />
    <result column="uuid" property="parentId" jdbcType="CHAR" />
    <result column="full_name" property="fullName" jdbcType="VARCHAR" />
    <result column="status" property="status" jdbcType="BIT" />
    <result column="description" property="description" jdbcType="VARCHAR" />
    <result column="del_flag" property="delFlag" jdbcType="BIT" />
    <result column="type" property="type" jdbcType="INTEGER" />
    <result column="short_en_name" property="shortEnName" jdbcType="VARCHAR" />
    <result column="change_size" property="changeSize" jdbcType="BIT" />
  </resultMap>


  <select id="querySourceByType" parameterType="java.lang.Integer" resultType="com.kingthy.dto.SourceTypeDTO">
    SELECT a.full_name name,a.uuid,a.parent_id parentId FROM source_category a WHERE a.type = #{type} AND a.del_flag = FALSE
  </select>



    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" suffix=")" prefixOverrides="and">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach collection="criterion.value" item="listItem" open="(" close=")"
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Update_By_Example_Where_Clause">
        <where>
            <foreach collection="example.oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" suffix=")" prefixOverrides="and">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach collection="criterion.value" item="listItem" open="(" close=")"
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Base_Column_List">
    id, create_date, modify_date, version, creator, modifier, parent_id, full_name,
    status, description, del_flag,type,short_en_name,change_size,uuid,grade,path
  </sql>

    <select id="findByPage" parameterType="com.kingthy.entity.SourceCategory" resultMap="BaseResultMap">
        SELECT id, create_date, modify_date, version, creator, modifier, parent_id, full_name,
        status, description, del_flag,type,short_en_name,change_size,uuid,grade,path
        FROM source_category
        WHERE del_flag IS FALSE
        <if test=" fullName !=null and fullName!='' ">
            AND full_name LIKE concat('%',#{fullName},'%')
        </if>
        <if test=" type != null">
            AND type = #{type}
        </if>
        ORDER BY create_date DESC
    </select>

    <select id="findByPage_COUNT" resultType="Long">
        SELECT COUNT(*)
        FROM source_category
        WHERE del_flag IS FALSE
        <if test=" fullName !=null and fullName!='' ">
            AND full_name LIKE concat('%',#{fullName},'%')
        </if>
        <if test=" type != null">
            AND type = #{type}
        </if>
    </select>



    <select id="querySubCategory" parameterType="int"
            resultType="com.kingthy.response.QuerySubCategoryResp">
      select
      sc.uuid,
      sc.full_name AS name
      from
      source_category sc
      where sc.type = #{categoryType}
      and sc.parent_id = 0
   </select>
</mapper>
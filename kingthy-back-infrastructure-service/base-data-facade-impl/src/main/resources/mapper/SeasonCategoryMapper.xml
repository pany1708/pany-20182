<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kingthy.mapper.SeasonCategoryMapper">

    <select id="findByPage" resultType="com.kingthy.entity.SeasonCategory" parameterType="com.kingthy.entity.SeasonCategory">
        SELECT id,create_date createDate,modify_date modifyDate,version,class_name className,
        uuid,status,parent_id parentId,description,orders,grade,tree_path treePath
        FROM season_category
        WHERE del_flag is FALSE
        <if test=" className!=null and className!='' ">
            AND class_name LIKE concat('%', #{className}, '%')
        </if>
        ORDER BY create_date DESC
    </select>

    <select id="findByPage_COUNT" resultType="Long" >
        SELECT count(uuid)
        FROM season_category
        WHERE del_flag is FALSE
        <if test=" className!=null and className!='' ">
            AND class_name LIKE concat('%', #{className}, '%')
        </if>
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kingthy.mapper.MaterialImagesMapper">
    <select id="queryUUidByMaterialUuid" resultType="java.lang.String" parameterType="java.lang.String">
        SELECT uuid FROM material_images s WHERE s.material_uuid = #{materialUuid} AND s.del_flag IS FALSE ORDER BY s.create_date

    </select>

    <select id="delImagesByMaterialUuid" resultType="java.lang.Integer" parameterType="java.lang.String">
        UPDATE material_images SET del_flag = 1,modify_date = NOW() WHERE material_uuid = #{materialUuid} AND del_flag IS FALSE
    </select>
</mapper>